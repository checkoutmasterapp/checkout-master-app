doctype html
html(lang="en")
  head
    meta(charset="utf-8")
    meta(content="width=device-width, initial-scale=1.0" name="viewport")
    -if(customize_checkout?.favicon !== undefined )
      link(rel="icon" type="image/x-icon", href=`${customize_checkout.favicon}`)
    -else
      link(href="/assets/img/fav.png" rel="icon")
    // Favicons
    link(href="/assets/img/fav.png" rel="apple-touch-icon")

    // Google Fonts
    link(rel="preconnect" href="https://fonts.gstatic.com")

    link(rel="stylesheet" href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,600,600i,700,700i|Nunito:300,300i,400,400i,600,600i,700,700i|Poppins:300,300i,400,400i,500,500i,600,600i,700,700i")
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css")
    link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/Swiper/6.8.4/swiper-bundle.min.css")
    // Vendor CSS Files
    link(rel="stylesheet" href="/assets/css/bootstrap.min.css")
    link(rel="stylesheet" href="/assets/css/bootstrap-icons.css")
    link(rel="stylesheet" href="/assets/css/bootstrap-notify.css")

    // Template Main CSS File
    link(rel="stylesheet" href="/assets/css/style.css")
    link(rel="stylesheet" href="/assets/css/developer.css")
    link(rel="stylesheet" href="/assets/css/shopify_css.css")
    link(rel="stylesheet" href="/assets/css/shopify/upsell.css")
    block header_style

    // Customize Checkout (Custom Scripts) Start 
    .
      !{custom_script}
    // Customize Checkout (Custom Scripts) End

    script.
      (function(w){w.fpr=w.fpr||function(){w.fpr.q = w.fpr.q||[];w.fpr.q[arguments[0]=='set'?'unshift':'push'](arguments);};})(window);
      fpr("init", {cid:"n2s3pd1g"});
      fpr("click");
    script(src='https://cdn.firstpromoter.com/fpr.js' async='')

    // Meta Pixel Code
    script.
      !function(f,b,e,v,n,t,s)
      {if(f.fbq)return;n=f.fbq=function(){n.callMethod?
      n.callMethod.apply(n,arguments):n.queue.push(arguments)};
      if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
      n.queue=[];t=b.createElement(e);t.async=!0;
      t.src=v;s=b.getElementsByTagName(e)[0];
      s.parentNode.insertBefore(t,s)}(window, document,'script', 'https://connect.facebook.net/en_US/fbevents.js');
      fbq('init', '626573416008665');
      fbq('track', 'PageView');
    noscript
      img(height="1" width="1" style="display:none" src="https://www.facebook.com/tr?id=626573416008665&ev=PageView&noscript=1")
    // End Meta Pixel Code

    // Google tag (gtag.js)
    script(async='' src='https://www.googletagmanager.com/gtag/js?id=G-5Y7MGBK12N')
    script.
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());
      gtag('config', 'G-5Y7MGBK12N');
    // End Google tag (gtag.js)

    block header_scripts

    script.
      let store_url = `!{store_id}`;
      let ajax_url = `!{process.env.APP_URL}`;

    body(class=`shopify_body ${body_class ? body_class : ""}`)
      .shopify-loader
        .shopify-loader-spinner
          .bar1
          .bar2
          .bar3
      block content

    footer
      .footer_bottom_scroll 

    script(src='/assets/js/jquery.js')
    script(src='https://cdnjs.cloudflare.com/ajax/libs/Swiper/6.8.4/swiper-bundle.min.js')
    script(src='/assets/js/bootstrap.min.js')
    script(src='/assets/js/jquery.validate.min.js')
    script(src='/assets/js/bootstrap.bundle.js')
    script(src='/assets/js/bootstrap-notify.min.js')
    script(type="text/javascript" src="/assets/js/jquery.inputmask.bundle.min.js")
    script(type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/4.6.0/socket.io.js")

    block scripts